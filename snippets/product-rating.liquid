{{ 'component-rating.css' | asset_url | stylesheet_tag }}

<!-- Rating section -->
<div class="product-rating {{ class }}">
  <!-- Start of Judge.me code -->
  <div
    style="{{ jm_style }}"
    class="jdgm-widget jdgm-preview-badge"
    data-id="{{ product.id }}"
    data-auto-install="false"
  >
    {{ product.metafields.judgeme.badge }}
  </div>
</div>
